server:
  port: 8880
  undertow:
    io-threads: 2
    worker-threads: 200

spring:
  mvc:
    favicon:
      enabled: false
    view:
      prefix: /
      suffix: .html
  application:
    name: moon-consumer
  profiles:
    include:
    - web
    - locale
  data:
    mongodb:
      uri: mongodb://localhost:27017/test
  mail:
    host: smtp.qq.com
    port: 465
    username: 357444224@qq.com
    password: ukecrawoanancaaa
    properties:
      mail:
        smtp:
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory
            fallback: false
          auth: true
          timeout: 25000

mybatis:
  typeAliasesPackage : "cn.minqi.consumer.entity"
  #checkConfig-location : false
  mapper-locations : "classpath:cn/minqi/consumer/mapper/xml/*Mapper.xml"


logging:
  level:
    org.springframework.web: INFO
    cn.minqi: DEBUG
  pattern:
    console: "%date{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger{36})  - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  file: logs/application.log
